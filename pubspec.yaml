name: control_key_gen
description: "تطبيق مولّد مفاتيح التفعيل المخصص لتطبيق الأتمتة المصغرة للإختبارات"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  # مكتبة التشفير لتوليد المفاتيح بنفس خوارزمية SHA-256
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # دعم الخطوط العربية
  # fonts:
  #   - family: Cairo
  #     fonts:
  #       - asset: assets/fonts/Cairo-Regular.ttf
